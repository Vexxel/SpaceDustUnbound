SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_Trappist-1]
{
	resourceName = Antimatter
	body = Trappist1

	RESOURCEBAND
	{
		name = starInnerBelt
		title = #LOC_SpaceDust_Band_LowBelt

		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.000000000000000000000000082
		maxAbundance = 0.000000000000000000000000210

		// Visuals Data
		// -------------
		countScale = 75
		rotateRate = 0.25
		
		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 60000000
		altLowerBound = 55000000
		// peak of a distribution
		altPeak = 57500000
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 5000000
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 20
		// Minimum latitude for a distribution
		latLowerBound = -20
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 6
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
	RESOURCEBAND
	{
		name = belphZone
		title = #LOC_SpaceDust_Band_HighBelt

		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.000000000000000000000000327
		maxAbundance = 0.000000000000000000000000516

		// Visuals Data
		// -------------
		countScale = 150
		rotateRate = 0.25
		
		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 167000000
		altLowerBound = 161000000
		// peak of a distribution
		altPeak = 164000000
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 100000
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 4
		// Minimum latitude for a distribution
		latLowerBound = -4
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 0
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_Trappist-1]
{
  resourceName = LqdHydrogen
  body = Trappist1

  RESOURCEBAND
  {
    name = starExo
	title = #LOC_SpaceDust_Band_Exosphere
	
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000000000000000010
    maxAbundance = 0.0000000000000000021

	
	countScale = 50
	rotateRate = 1

    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 75000000
    // minimum altitude for a distribution
    altLowerBound = 10000000
    // peak of a distribution
    altPeak = 10000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_Trappist-1&Blueshift]
{
	resourceName = Graviolium
	body = Trappist1

	RESOURCEBAND
	{
		name = starGraviolium
		title = #LOC_SpaceDust_Band_Ring

		minAbundance = 0.000000000000170
		maxAbundance = 0.000000000000220
		
		countScale = 500
		rotateRate = 5

		alwaysDiscovered = false
		alwaysIdentified = false
		allowRemoteDiscovery = true
		allowRemoteIdentification = true
		remoteDiscoveryScale = 0.1
		remoteIdentifyScale = 0.1

		useAirDensity = false
		distributionType = Spherical

		altLowerBound = 	60000000
		altUpperBound = 	80000000
		altPeak = 			70000000
		altVariability = 	  500000
		altFalloffType = 	Linear

		latUpperBound = 2
		latLowerBound = -2
		latPeak = 0
		latVariability = 0
		latFalloffType = Linear
	}
}