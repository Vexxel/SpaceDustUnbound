SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_TauCeti]
{
	resourceName = Antimatter
	body = TauCeti

	RESOURCEBAND
	{
		name = starInnerBelt
		title = #LOC_SpaceDust_Band_LowBelt

		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.000000000000000000000000142
		maxAbundance = 0.000000000000000000000000220

		// Visuals Data
		// -------------
		countScale = 75
		rotateRate = 0.25
		
		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 1550000000
		altLowerBound = 1435000000
		// peak of a distribution
		altPeak = 1475000000
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 18000000
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 25
		// Minimum latitude for a distribution
		latLowerBound = -25
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 6
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
	RESOURCEBAND
	{
		name = starOuterBelt
		title = #LOC_SpaceDust_Band_HighBelt

		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.000000000000000000000000242
		maxAbundance = 0.000000000000000000000000454

		// Visuals Data
		// -------------
		countScale = 150
		rotateRate = 0.25
		
		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 4750000000
		altLowerBound = 4250000000
		// peak of a distribution
		altPeak = 4400000000
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 17200000
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 3
		// Minimum latitude for a distribution
		latLowerBound = -3
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 0
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_TauCeti]
{
  resourceName = LqdHydrogen
  body = TauCeti

  RESOURCEBAND
  {
    name = starExo

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000000000000000000011
    maxAbundance = 0.0000000000000000000026

    title = #LOC_SpaceDust_Band_Exosphere

    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false
	
	countScale = 50
	rotateRate = 1

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 1800000000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 1000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE:NEEDS[Blueshift&SpaceDust&GU/_Systems/_TauCeti]
{
	resourceName = Graviolium
	body = TauCeti

	RESOURCEBAND
	{
		name = starGraviolium
		title = #LOC_SpaceDust_Band_Ring

		minAbundance = 0.00000000000199
		maxAbundance = 0.00000000000282
		
		countScale = 1000
		rotateRate = 5

		alwaysDiscovered = false
		alwaysIdentified = false
		allowRemoteDiscovery = true
		allowRemoteIdentification = true
		remoteDiscoveryScale = 0.1
		remoteIdentifyScale = 0.1

		useAirDensity = false
		distributionType = Spherical

		altLowerBound = 	1720000000
		altUpperBound = 	2420000000
		altPeak = 			1920000000
		altVariability = 	45000000
		altFalloffType = 	Linear

		latUpperBound = 2
		latLowerBound = -2
		latPeak = 0
		latVariability = 0
		latFalloffType = Linear
	}
}