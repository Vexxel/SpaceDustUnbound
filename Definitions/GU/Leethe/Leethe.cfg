SPACEDUST_RESOURCE:NEEDS[SpaceDust]
{
	resourceName = Antimatter
	body = Lich

	RESOURCEBAND
	{
		name = starInnerBelt
		title = #LOC_SpaceDust_Band_LowBelt

		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		
		minAbundance = 0.000000000000000000000000420
		maxAbundance = 0.00000000000000000000000069

		// Visuals Data
		// -------------
		countScale = 500
		rotateRate = 1
		
		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		altUpperBound = 1333333333
		altLowerBound =  999999999
		altPeak = 		1111111111
		
		// peak of a distribution
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 5000000
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 45
		// Minimum latitude for a distribution
		latLowerBound = -45
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 3
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
	RESOURCEBAND
	{
		name = starOuterBelt
		title = #LOC_SpaceDust_Band_HighBelt

		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		
		minAbundance = 0.000000000000000000000000000262
		maxAbundance = 0.000000000000000000000000000345

		// Visuals Data
		// -------------
		countScale = 500
		rotateRate = 1
		
		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		altUpperBound = 4500000000
		altLowerBound = 4200000000
		altPeak = 		4350000000
		
		// peak of a distribution
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 25000000
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 45
		// Minimum latitude for a distribution
		latLowerBound = -45
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 3
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
}

SPACEDUST_RESOURCE:NEEDS[Blueshift&SpaceDust]
{
	resourceName = Graviolium
	body = Lich

	RESOURCEBAND
	{
		name = starGraviolium
		title = #LOC_SpaceDust_Band_Generic

		minAbundance = 0.0000000000950
		maxAbundance = 0.0000000001220
		
		countScale = 100
		rotateRate = 5

		alwaysDiscovered = false
		alwaysIdentified = false
		allowRemoteDiscovery = true
		allowRemoteIdentification = true
		remoteDiscoveryScale = 0.1
		remoteIdentifyScale = 0.1

		useAirDensity = false
		distributionType = Spherical

		altLowerBound = 	10000000
		altUpperBound = 	1500000000
		altPeak = 			50000000
		altVariability = 	  5000000
		altFalloffType = 	Linear

		latUpperBound = 70
		latLowerBound = -70
		latPeak = 0
		latVariability = 0
		latFalloffType = Linear
	}
}