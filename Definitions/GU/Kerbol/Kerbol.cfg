SPACEDUST_RESOURCE
{
	resourceName = Antimatter
	body = Sun

	RESOURCEBAND
	{
		name = sunInnerBelt
		title = #LOC_SpaceDust_Band_LowBelt

		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.000000000000000000000000162
		maxAbundance = 0.000000000000000000000000345

		// Visuals Data
		// -------------
		countScale = 75
		rotateRate = 0.25
		
		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 600000000
		altLowerBound = 525000000
		// peak of a distribution
		altPeak = 565000000
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 20000000
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 10
		// Minimum latitude for a distribution
		latLowerBound = -10
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 6
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
	RESOURCEBAND
	{
		name = sunOuterBelt
		title = #LOC_SpaceDust_Band_HighBelt

		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.000000000000000000000000352
		maxAbundance = 0.000000000000000000000000585

		// Visuals Data
		// -------------
		countScale = 150
		rotateRate = 0.25
		
		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 2500000000
		altLowerBound = 2300000000
		// peak of a distribution
		altPeak = 2400000000
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 0
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 3
		// Minimum latitude for a distribution
		latLowerBound = -3
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 0
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
}

SPACEDUST_RESOURCE
{
	resourceName = Rock
	body = Sun

	RESOURCEBAND
	{
		name = SunExo
		title = Ring (Duna)

		minAbundance = 0.008
		maxAbundance = 0.015
		
		// Visuals Data
		countScale = 300
		rotateRate = 1

		// Discoverability Data
		alwaysDiscovered = false
		alwaysIdentified = false
		discoveryScienceReward = 10 // 5
		identifyScienceReward = 10 // 5

		useAirDensity = False
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		altUpperBound = 32000000000
		altLowerBound = 14000000000
		altPeak = 23000000000
		altVariability = 0
		altFalloffType = Linear

		latUpperBound = 6
		latLowerBound = -6
		latPeak = 0
		latVariability = 0
		latFalloffType = Linear
	}
}

