SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_EpsilonEridani]
{
  resourceName = Antimatter
  body = Rhan
  RESOURCEBAND
  {
    name = rhanRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
       minAbundance = 1.1E-29
       maxAbundance = 2.2E-29


    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 580000
    // minimum altitude for a distribution
    altLowerBound = 525000
    // peak of a distribution
    altPeak = 560000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 35
    // Minimum latitude for a distribution
    latLowerBound = -35
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

  RESOURCEBAND
  {
    name = rhanRingHigher
    title = #LOC_SpaceDust_Band_HighBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 4.1E-29
    maxAbundance = 9.4E-29

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2000000
    // minimum altitude for a distribution
    altLowerBound = 1800000
    // peak of a distribution
    altPeak = 1900000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 1

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 35
    // Minimum latitude for a distribution
    latLowerBound = -35
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_EpsilonEridani]
{
  resourceName = ArgonGas
  body = Rhan
  
  RESOURCEBAND
  {
    name = rhanAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = true

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000014
    maxAbundance = 0.000016

    // Scale abundances by air density
    useAirDensity = True
    // 
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 55000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_EpsilonEridani]
{
  resourceName = LqdHydrogen
  body = Rhan

  RESOURCEBAND
  {
    name = rhanExo
    title = #LOC_SpaceDust_Band_Exosphere
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000000000000000000805
    maxAbundance = 0.00000000000000000002405


     // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 4200000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 450000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_EpsilonEridani]
{
  resourceName = Oxidizer
  body = Rhan
  
  RESOURCEBAND
  {
    name = rhanAtmo
    // Localized Names
    title = #LOC_SpaceDust_Band_Atmosphere
      
    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = true

    // Abundance Data
    // --------------
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.000356
    maxAbundance = 0.000406


    // Scale abundances by air density
    useAirDensity = True
    // Map air density to scaling factors
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }

    // Distribution Data
    // --------------

    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 55000
    // minimum altitude for a distribution
    altLowerBound = -500
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_EpsilonEridani]
{
  resourceName = XenonGas
  body = Rhan


  RESOURCEBAND
  {
    name = rhanAtmo
    title = #LOC_SpaceDust_Band_Atmosphere
    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000000100
    maxAbundance = 0.00000000220


    
    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = true

    
    // Scale abundances by air density
    useAirDensity = True
    // Map air density to scaling factors
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 55000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_EpsilonEridani]
{
	resourceName = LqdCO2
	body = Rhan

	RESOURCEBAND
	{
		name = rhanAtmo

		minAbundance = 0.000012
		maxAbundance = 0.000014
		title = #LOC_SpaceDust_Band_LowAtmosphere

		// Discoverability Data
		alwaysDiscovered = true
		alwaysIdentified = true

		useAirDensity = False
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		altUpperBound = 11000
		altLowerBound = -500
		altPeak = 6000
		altVariability = 0
		altFalloffType = Linear

		latUpperBound = 90
		latLowerBound = -90
		latPeak = 0
		latVariability = 0
		latFalloffType = None
	}
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_EpsilonEridani]
{
	resourceName = LqdNitrogen
	body = Rhan

	RESOURCEBAND
	{
		name = rhanAtmo
		title = #LOC_SpaceDust_Band_Atmosphere

		// Discoverability Data
		// --------------
		alwaysDiscovered = true
		alwaysIdentified = true

		// Abundance Data
		// --------------
		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 1.1925 // 81 / 80  (guess-work by JadeOfMaar)
		maxAbundance = 1.25 // 84 / 80

		// Scale abundances by air density
		useAirDensity = True
		// 
		densityCurve
		{
			key = 0 0
			key = 1 1
			key = 12 12
		}

		// Distribution Data
		// --------------

		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 55000
		// minimum altitude for a distribution
		altLowerBound = -500
		// peak of a distribution
		altPeak = 0
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 0
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 90
		// Minimum latitude for a distribution
		latLowerBound = -90
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 0
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
}

SPACEDUST_RESOURCE:NEEDS[SpaceDust&GU/_Systems/_EpsilonEridani]
{
	resourceName = Water
	body = Rhan

	RESOURCEBAND
	{
		name = rhanAtmo
		title = #LOC_SpaceDust_Band_LowAtmosphere

		minAbundance = 0.0005
		maxAbundance = 0.0005

		// Discoverability Data
		alwaysDiscovered = true
		alwaysIdentified = true

		useAirDensity = False
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		altUpperBound = 10000
		altLowerBound = -500
		altPeak = 5500
		altVariability = 0
		altFalloffType = Linear

		latUpperBound = 90
		latLowerBound = -90
		latPeak = 0
		latVariability = 0
		latFalloffType = None
	}
}
